//Implement local message storage that checks user_id to load messages for that user from local storage. Every sent/recieved message should be stored in local storage and assigned to a chat consisting of 2 users.

